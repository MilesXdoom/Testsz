const serviceBtns = document.querySelectorAll('.service-btn'); serviceBtns.forEach(btn => { btn.addEventListener('click', event => { const service = event.target.dataset.service; document.querySelectorAll('.service').forEach(el => el.classList.remove('active')); document.getElementById(service).classList.add('active'); }); }); // Seleccionar todos los enlaces del menú de navegación const navLinks = document.querySelectorAll('nav a'); // Agregar un controlador de eventos para cada enlace del menú de navegación navLinks.forEach(link => { link.addEventListener('click', event => { // Prevenir el comportamiento predeterminado del enlace event.preventDefault(); // Obtener el ID de la sección objetivo const targetId = event.target.hash; // Desplazarse suavemente hasta la sección objetivo document.querySelector(targetId).scrollIntoView({ behavior: 'smooth' }); }); }); 